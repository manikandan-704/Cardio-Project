server:
  port: 8082

spring:
  application:
    name: ActivityService

  data:
    mongodb:
      uri: mongodb://localhost:27017/ActivityMicroService
      database: ActivityService

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/